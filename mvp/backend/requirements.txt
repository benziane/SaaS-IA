# SaaS-IA MVP - Python Dependencies
# Updated: 2026-03-23

# FastAPI & Server
fastapi==0.135.1
uvicorn[standard]==0.42.0

# Database
sqlmodel==0.0.37
psycopg2-binary==2.9.9
asyncpg==0.31.0
aiosqlite==0.22.1
alembic==1.18.4

# Cache & Queue
redis==5.2.1
celery[redis]==5.6.2
flower==2.0.1

# Payments
stripe==8.0.0

# Validation & Settings
pydantic==2.12.5
pydantic-settings==2.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.22

# AI Services
assemblyai==0.58.0
yt-dlp>=2026.3.17
youtube-transcript-api>=0.6.0
faster-whisper>=1.0.0
pyannote.audio>=3.1.0
streamlink>=6.0.0
moviepy>=2.0.0
ffmpeg-python>=0.2.0
pydub>=0.25.0
noisereduce>=3.0.0
Pillow>=10.0.0

# Vector Search & Embeddings
pgvector>=0.3.0
sentence-transformers>=3.0.0

# LLM Proxy & Cost Tracking
litellm>=1.50.0

# Security & PII Detection
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
nemoguardrails>=0.10.0

# Voice (open-source TTS)
TTS>=0.22.0

# PDF Processing (auto-detect: PyMuPDF preferred, pdfplumber fallback)
PyMuPDF>=1.24.0
pdfplumber>=0.11.0

# Data Analysis
duckdb>=1.0.0
ydata-profiling>=4.6.0

# Graph & Agent Engine
networkx>=3.2.0

# Sentiment (local model - optional, falls back to LLM)
transformers>=4.40.0

# Content Readability (optional)
textstat>=0.7.0

# Image Upscaling (optional, falls back to no-op)
realesrgan>=0.3.0
basicsr>=1.4.0

# Fine-Tuning (optional - heavy deps, falls back to mock)
# unsloth>=2024.0  # Uncomment when GPU available
# lm-eval>=0.4.0   # Uncomment when GPU available

# WebRTC Realtime (optional, falls back to text-based)
livekit-server-sdk>=0.6.0

# Code Sandbox isolation (optional - falls back to subprocess)
docker>=7.0.0

# AI Providers
google-generativeai==0.8.6
anthropic==0.86.0
openai==1.54.5  # Pinned v1.x for Groq compatibility

# Persistent AI Memory (optional - falls back to DB queries)
mem0ai>=0.1.0

# Search (optional - falls back to PostgreSQL)
meilisearch>=0.31.0

# Web Crawling
crawl4ai>=0.4.0

# GitHub Repo Scraping & AI Packaging
skill-seekers>=0.1.0

# Utils
python-dotenv==1.2.2
structlog==25.5.0
python-dateutil>=2.9.0

# Monitoring & Observability
prometheus-client==0.24.1
pyyaml==6.0.3

# LLM Observability (Langfuse - optional, falls back to DB-only monitoring)
langfuse>=2.0.0

# Enterprise Observability (OpenTelemetry)
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-grpc>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-asyncpg>=0.48b0
opentelemetry-instrumentation-redis>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0

# Error Tracking (Sentry/GlitchTip)
sentry-sdk[fastapi]>=2.14.0

# Compression
brotli>=1.1.0

# Rate Limiting
slowapi==0.1.9

# Development (optional)
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==7.1.0
httpx==0.28.1
ruff==0.15.8
black==26.3.1
bandit==1.9.4
mypy==1.19.1
