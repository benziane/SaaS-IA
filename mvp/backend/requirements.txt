# SaaS-IA MVP - Python Dependencies
# Python 3.14 — versions non-pinned, pip résout les compatibilités
# Packages incompatibles Python 3.14 commentés avec fallback noté

# FastAPI & Server
fastapi
uvicorn[standard]

# Database
sqlmodel
psycopg[binary]
asyncpg
aiosqlite
alembic

# Cache & Queue
redis
celery[redis]
flower

# Payments
stripe

# Validation & Settings
pydantic[email]
pydantic-settings

# Auth
python-jose[cryptography]
bcrypt
python-multipart

# AI Services
assemblyai
yt-dlp
youtube-transcript-api
faster-whisper
pyannote.audio
streamlink
instaloader
moviepy
ffmpeg-python
pydub
noisereduce
Pillow

# Vector Search & Embeddings
pgvector
sentence-transformers

# LLM Proxy & Cost Tracking
litellm

# Security & PII Detection
presidio-analyzer
presidio-anonymizer
nemoguardrails

# Voice TTS — Coqui TTS (TTS>=0.22.0) incompatible Python 3.14, fallback: OpenAI TTS
# TTS

# PDF Processing
PyMuPDF
pdfplumber

# Data Analysis
duckdb
# ydata-profiling — bloqué <3.14, fallback pandas dans data_analyst

# Graph & Agent Engine
networkx

# Sentiment (local model)
transformers
torch

# Content Readability
textstat

# Image Upscaling — realesrgan/basicsr incompatibles Python 3.14, fallback: sans upscaling
# realesrgan
# basicsr

# Fine-Tuning (GPU uniquement)
# unsloth
# lm-eval

# WebRTC Realtime — livekit-server-sdk sans wheel Python 3.14, fallback: text-based
# livekit-server-sdk

# Code Sandbox
docker

# AI Providers
google-generativeai
anthropic
openai

# Persistent AI Memory
mem0ai

# Search
meilisearch

# Web Crawling — lxml forced to 6.x via Dockerfile override (crawl4ai/streamlink pin lxml<6)
crawl4ai

# GitHub Repo Scraping
skill-seekers

# Instagram Intelligence — instagrapi pins pydantic strictement, fallback: instaloader
# instagrapi

# Utils
python-dotenv
structlog
python-dateutil

# Monitoring & Observability
prometheus-client
pyyaml

# LLM Observability
langfuse

# Enterprise Observability (OpenTelemetry)
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-grpc
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-asyncpg
opentelemetry-instrumentation-redis
opentelemetry-instrumentation-httpx

# Error Tracking
sentry-sdk[fastapi]

# Compression
brotli

# Rate Limiting
slowapi

# Development
pytest
pytest-asyncio
pytest-cov
httpx
ruff
black
bandit
mypy
