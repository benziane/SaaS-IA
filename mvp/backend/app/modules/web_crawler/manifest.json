{
  "name": "web_crawler",
  "version": "6.0.0",
  "description": "Full crawl4ai surface: fit_markdown, CSS/XPath/regex extraction, AI schema gen, raw HTML processing, batch crawl, deep crawl (BFS/DFS/Best-First + filters + resume), seed URLs (BM25), adaptive crawl (self-tuning), HTTP-only scrape, CrawlerHub profiles, geolocation, undetected browser, LLM table extraction",
  "prefix": "/api/crawler",
  "tags": ["web_crawler"],
  "enabled": true,
  "dependencies": ["crawl4ai"]
}
